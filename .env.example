#--------------------------------------------------------------------
# Moonstar School Management System - Environment Configuration
#--------------------------------------------------------------------
# This file contains the environment configuration for the Moonstar SMS
# Copy this file to .env and update the values according to your setup
#--------------------------------------------------------------------

#--------------------------------------------------------------------
# ENVIRONMENT
#--------------------------------------------------------------------
# Options: development, production, testing
# For production, set to 'production' to disable debugging and errors
CI_ENVIRONMENT = development

#--------------------------------------------------------------------
# APP
#--------------------------------------------------------------------
# Base URL for your application (include trailing slash)
# Example: http://localhost:8080/ or https://moonstar.example.com/
app.baseURL = 'http://localhost:8080/'

# Force HTTPS in production
# app.forceGlobalSecureRequests = true

# Enable Content Security Policy (recommended for production)
# app.CSPEnabled = false

#--------------------------------------------------------------------
# DATABASE - MySQL Configuration
#--------------------------------------------------------------------
# Update these values with your MySQL database credentials
database.default.hostname = localhost
database.default.database = moonstar_db
database.default.username = root
database.default.password = 
database.default.DBDriver = MySQLi
database.default.DBPrefix = 
database.default.port = 3306
database.default.charset = utf8mb4
database.default.DBCollat = utf8mb4_general_ci

# PRODUCTION NOTE: 
# - Create a dedicated MySQL user with limited privileges
# - Use strong passwords (never leave password empty in production)
# - Consider using environment-specific databases
# Example production settings:
# database.default.hostname = your-db-host.example.com
# database.default.database = moonstar_production
# database.default.username = moonstar_user
# database.default.password = your-strong-password-here

#--------------------------------------------------------------------
# ENCRYPTION
#--------------------------------------------------------------------
# Generate a secure encryption key using: php spark key:generate
# This is required for session encryption and other security features
# NEVER commit your actual key to version control!
# encryption.key = your-32-character-encryption-key-here

#--------------------------------------------------------------------
# SESSION
#--------------------------------------------------------------------
# Session handler (Files, Database, Redis, Memcached)
session.driver = 'CodeIgniter\Session\Handlers\FileHandler'
session.savePath = WRITEPATH . 'session'
session.cookieName = 'moonstar_session'
session.expiration = 7200
session.matchIP = false

#--------------------------------------------------------------------
# LOGGER
#--------------------------------------------------------------------
# Log threshold levels: 0=Emergency, 1=Alert, 2=Critical, 3=Error, 
#                       4=Warning, 5=Notice, 6=Info, 7=Debug, 8=All
# For production, use threshold 3 (Error) or 4 (Warning)
logger.threshold = 4

#--------------------------------------------------------------------
# UPLOAD SETTINGS
#--------------------------------------------------------------------
# Maximum upload file size (in MB)
# app.maxUploadSize = 10

#--------------------------------------------------------------------
# PRODUCTION CHECKLIST
#--------------------------------------------------------------------
# Before deploying to production:
# [ ] Set CI_ENVIRONMENT to 'production'
# [ ] Update app.baseURL to your production domain
# [ ] Enable app.forceGlobalSecureRequests = true (for HTTPS)
# [ ] Set strong database credentials
# [ ] Generate and set encryption.key
# [ ] Set logger.threshold to 3 or 4
# [ ] Enable app.CSPEnabled for security
# [ ] Disable error display (handled automatically in production mode)
# [ ] Set up proper file/folder permissions (writable/cache/logs/session)
# [ ] Configure proper backup strategy for database
# [ ] Set up SSL certificate
# [ ] Configure firewall rules
